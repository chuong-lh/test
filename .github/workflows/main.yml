name: Run echo

on:
  workflow_dispatch:
    inputs:
      # environment:
      #   description: 'Environment to run tests against'
      #   required: true
      #   type: choice
      #   default: 'staging'
      #   options:
      #   - staging
      #   - production
      environment:
        description: 'Environment to run against'
        type: environment
        default: development
        required: true
      datapack_only:
        description: 'A Test Case is a set of actions executed to verify a particular feature or functionality of your software application.'
        required: true
        type: boolean
        default: false

jobs:
  development:
    # uses: ./.github/workflows/template.yml
    # with:
    #   environment: development
    #   build_options: "'${{ inputs.test_cases }} && ls -a'"
    #   image_repository: chuong
  # chuong:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Print the input name to STDOUT
  #       run: ls -a

    runs-on: ubuntu-latest
    steps:
    - uses: 'actions/checkout@ac593985615ec2ede58e132d2e21d2b1cbd6127c'
    - name: 'Setup DataPack Config'
      env:
        CI_ENVIRONMENT_NAME: ${{ inputs.environment }}
      run: |
        echo "development"
  production:
    if: inputs.datapack_only == 'false'
    runs-on: ubuntu-latest
    steps:
    - name: 'Setup production'
      run: |
        echo "production"
